let names = ['Michael', 'Melissa', 'Andrea'];

let values = [true, {}, [], 2, 'awesome'];

/* Notes
When you need order.  
When you need fast access/insertion and removal(sort of...)

Insertion - It depends... (if pushing to the end it's O(n) and if pushing to the beginning its O(n) )
Removal - It depends... (if removing from the beginning its O(n) and if removing from the end its O(1))
Searching - O(n)
Access - O(1)



*/
